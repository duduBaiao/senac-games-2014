game.module(
    'jokenpo.domain'
)
.body(function() {
    
    Weapon = game.Class.extend({
    });
    
    Player = game.Class.extend({
    });
    
    CPU = game.Class.extend({
    });
    
    JoKenPo = game.Class.extend({
    });

});