game.module(    'game.objects.car').body(function(){        Car = game.Class.extend({                init: function(options) {                        this.sprite = new game.Sprite(options.imageName, 0, 0, {anchor: {x:0.5, y:0.5}});                        game.scene.map.container.addChild(this.sprite);                        game.scene.addObject(this);        }                //implementar as regras para os carros seguirem        //as ruas e fazer as curvas automaticas    });});