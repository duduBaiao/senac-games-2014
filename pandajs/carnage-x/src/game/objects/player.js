game.module(    'game.objects.player').require(    'game.objects.car').body(function(){        Player = Car.extend({                init: function() {                        this._super({imageName: 'redCar'});                        this.sprite.position.x = game.scene.map.spawnAt.x;            this.sprite.position.y = game.scene.map.spawnAt.y;                        this.rotateToDirection(game.scene.map.spawnAt.direction);        }    });});